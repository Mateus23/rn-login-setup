import { Button, View, Text } from 'react-native';

const HomeScreen = ({ navigation }) => {
    return (
      <View>
        <Text>
          Home
        </Text>
        <Button
          title="Button"
          onPress={() => {}}
        />
      </View>
    );
  }

export default HomeScreen;