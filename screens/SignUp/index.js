import { Button, View, Text } from 'react-native';

const SignUpScreen = ({ navigation }) => {
    return (
      <View>
        <Text>
          Signup
        </Text>
        <Button
          title="Button"
          onPress={() => {}}
        />
      </View>
    );
  }

export default SignUpScreen;