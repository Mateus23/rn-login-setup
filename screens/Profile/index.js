import { Button, View, Text } from 'react-native';

const ProfileScreen = ({ navigation }) => {
    return (
      <View>
        <Text>
          Profile
        </Text>
        <Button
          title="Button"
          onPress={() => {}}
        />
      </View>
    );
  }

export default ProfileScreen;